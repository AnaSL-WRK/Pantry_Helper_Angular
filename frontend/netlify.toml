[build]
  command   = "npm install && npm run build -- --configuration production"
  publish   = "dist/pantry-helper"

[build.environment]
  NODE_VERSION = "20"

 
[[redirects]]
  from   = "/*"
  to    = "/index.html"
  status = 200
